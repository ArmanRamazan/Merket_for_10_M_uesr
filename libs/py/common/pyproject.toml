[project]
name = "common"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "asyncpg>=0.30",
    "pydantic-settings>=2.7",
    "pyjwt>=2.10",
    "prometheus-client>=0.21",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
